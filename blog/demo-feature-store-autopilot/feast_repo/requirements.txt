# Feast Core
feast==0.39.0

# DuckDB for offline store
duckdb==1.0.0

# Ibis for DuckDB queries (required by Feast DuckDB offline store)
ibis-framework[duckdb]

# Redis for online store
redis==5.0.0

# Parquet support
pyarrow==16.0.0
pandas==2.2.0

# S3 support (for MinIO)
boto3==1.34.0
